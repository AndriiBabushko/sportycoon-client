{
  "name": "sportycoon-client",
  "private": true,
  "scripts": {
    "remove:node_modules": "rm -rf node_modules && rm -rf apps/admin/node_modules && rm -rf apps/main/node_modules && rm -rf apps/docs/node_modules && rm -rf packages/eslint-config/node_modules && rm -rf packages/ui/node_modules",
    "reinstall:deps": "pnpm run remove:node_modules && pnpm i",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@sportycoon/eslint-config": "workspace:*",
    "@sportycoon/typescript-config": "workspace:*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
